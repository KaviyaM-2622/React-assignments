// The UI triggers an action when something happens.

// The action is sent (dispatched) to the store.

// The reducer looks at the action and current state.

// The reducer returns a new state based on the action.

// The UI updates automatically with the new state.